
<mapper namespace="com.example.repository.BoardRepository">
    <insert id="insertBoard" resultType="Board">
        INSERT INTO board (title, content, createdTime) VALUES (#{title}, #{content}, #{createdTime})
    </insert>
</mapper>